export interface ImageModel {
    title: string
    format: string
    size: string
    caption: string
    tags: string
    owner: string
    location: string
    lastModifiedDateTime: number
    uploadedDateTime: number
}
